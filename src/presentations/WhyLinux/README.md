# Why Linux?

Available at `/why-linux` and linked from the homepage.

Imported from [glitchedmob/why-linux-slides](https://github.com/glitchedmob/why-linux-slides), commit `ce7b657668e5353012932f94f7bbbf10a2e1dbaa`.

Edit the 10 Markdown files in `slides/`. `WhyLinux.vue` lists them in the original presentation order, including `slide-2b` and `slide-3b`. Vite bundles their contents, so the deck does not fetch slides from GitHub or the original Netlify site.

The shared Reveal wrapper enables the Markdown plugin for this deck. `mint-theme.scss` retains the original Mint-style panels and Google Fonts, with its styles restricted to `.why-linux` so they do not affect other presentations.
